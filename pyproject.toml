[project]
name = "linuxdo-agent"
version = "0.1.0"
description = "Agent-friendly Linux.do hot topics and discussion summarizer"
readme = "README.md"
authors = [
    { name = "Kada Liao", email = "kadaliao@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "beautifulsoup4>=4.13.4",
    "httpx>=0.28.1",
    "mcp>=1.10.1",
    "typer>=0.16.0",
]

[dependency-groups]
dev = [
    "pytest>=8.4.1",
    "respx>=0.22.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[project.scripts]
linuxdo-agent = "linuxdo_agent.cli:app"
linuxdo-agent-mcp = "linuxdo_agent.mcp_server:main"

[build-system]
requires = ["hatchling>=1.27.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["linuxdo_agent"]
